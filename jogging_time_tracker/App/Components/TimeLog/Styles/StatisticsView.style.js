/* @flow*/
import { StyleSheet } from 'react-native'

export default StyleSheet.create({
  container: {
    paddingTop: 60,
    paddingBottom: 20,
  },
  row: {
    flexDirection: 'row',
    justifyContent: 'space-between',
  },
})
